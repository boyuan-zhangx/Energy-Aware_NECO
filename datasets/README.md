# Datasets

The scripts expect MUAD data under `datasets/muad` by default. With `download: true` in `configs/default.yaml`, TorchUncertainty downloads or reuses the dataset cache automatically.

Keep large raw data out of version control.

